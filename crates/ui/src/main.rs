pub mod app;
pub mod tray;

fn main() {
    println!("Starting Mudfish DNS UI Process...");
    // Initialize UI application
    // Set up system tray
    // Connect to Service and Core via IPC
    // Run the UI main loop
}
