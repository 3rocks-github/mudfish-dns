pub mod args;
pub mod client;
pub mod cmd;

fn main() {
    println!("Starting Mudfish DNS CLI...");
    // 1. Parse command line arguments (e.g., via clap)
    // 2. Connect to Service or Core process via IPC
    // 3. Send command and await response
    // 4. Print result to the terminal
}
